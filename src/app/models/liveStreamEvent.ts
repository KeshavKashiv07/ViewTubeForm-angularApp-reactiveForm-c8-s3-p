// Create the type LiveStreamEvent with required attributes and export
export type LiveStreamEvent = {
    eventTitle?: string ; 
    eventDate?: string;
    fromTime?: string;
    toTime?: string;
    description?: string;
    guests?: string;
}